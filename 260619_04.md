# NikkoMusicHub 專案進度回報｜260619_04

> 本輪修復 Pi 安裝腳本中的 systemd 路徑問題，並把所有安裝文件中的舊網址更新為 `security-final` 分支。

---

## a. 整體專案進度

| 狀態 | 數量 | 比例 |
|---|---|---|
| 已完成 | 102 | 85.0% |
| 部分完成 | 3 | 2.5% |
| 暫緩（未開始） | 15 | 12.5% |

**備註子項目**：
- 安裝腳本修復：已完成（security-final 與 main 分支皆已更新）。
- Pi 登入驗證：等待使用者在 Pi 重新執行安裝腳本後確認。
- Cloud Production：已部署且可用 `nikkolh` / `topup30%off` 登入。
- 文件同步：已完成（README、AGENTS、SECURITY_FINAL_REPORT、OPERATIONS_MANUAL）。

---

## b. 本次變動項目

| 序號 | 項目 | 改變內容 | 如何驗收 | 預期效果 |
|---|---|---|---|---|
| 1 | 修正 install.sh systemd 路徑 | 原本腳本到 `/srv/nikko-music/app/systemd/` 找 service 檔，但實際檔案在 `/srv/nikko-music/app/app/systemd/`，已改為正確路徑 | 在 Pi 重新執行安裝指令，不再出現 `systemd unit ... missing` | 安裝能繼續完成，服務成功啟動 |
| 2 | 確保預設密碼寫入 | 若 `nikko.env` 沒有 `NIKKO_DEFAULT_PASSWORD`，自動補上 `topup30%off`；只有在缺少該變數時才改寫 `initial-admin-password` | 重新安裝後 `cat /srv/nikko-music/data/initial-admin-password` 顯示 `topup30%off` | 可用預設帳密登入 Pi |
| 3 | 更新所有文件安裝網址 | README、AGENTS、OPERATIONS_MANUAL、SECURITY_FINAL_REPORT 中的安裝指令與密碼說明已改為 `security-final` 與 `topup30%off` | 文件內搜尋 `main/install.sh` 應無結果；README 安裝指令為 security-final | 使用者不會再從文件複製到舊網址 |
| 4 | 同步 main 分支 | main 分支的 install.sh 與文件也已更新，並移除誤追蹤的 `.obsidian` | main 分支 README 安裝指令為 security-final | 舊網址最終也會拿到新版腳本 |

---

## c. 目前遺留問題

| 序號 | 問題 | 不處理影響 | 重要 | 風險 | 優先 |
|---|---|---|---|---|---|
| 1 | EMQX Dashboard 仍走 HTTP 18083 | 在不可信網路登入可能洩漏管理密碼 | ★★★★★ | ★★★★☆ | ★★★★★ |
| 2 | Broker 憑證缺少 SAN/AKI | 未來 TLS 工具可能拒連；目前可用 `NIKKO_MQTT_TLS_VERIFY=0` 繞過 | ★★★★☆ | ★★★☆☆ | ★★★☆☆ |

---

## d. 需我處理項目

| 序號 | 項目 | 沒完成原因 | 需要你執行 | 為什麼我無法自己做 | 給 GPT 的 prompt |
|---|---|---|---|---|---|
| 1 | 在 Pi 重新安裝並驗證登入 | 需要 SSH 到 Pi 並有 sudo 權限 | 執行：`curl -fsSL https://raw.githubusercontent.com/s7808200772/nikkomusichub/security-final/install.sh \| sudo bash` <br> 完成後開 `http://100.69.3.7:8080`，用 `nikkolh` / `topup30%off` 登入 | 我無法直接 SSH 到你的 Pi | 我在 Raspberry Pi 用 nikkolh 帳號 SSH 登入了，請幫我執行 `curl -fsSL https://raw.githubusercontent.com/s7808200772/nikkomusichub/security-final/install.sh \| sudo bash`，並確認最後顯示安裝完成與預設密碼 `topup30%off`，然後用瀏覽器開 `http://100.69.3.7:8080` 測試登入。 |
| 2 | EMQX Dashboard HTTPS / IP 限制 | 需要 broker 主機權限 | 在 114.55.1.51 啟用 HTTPS 18084 或限制 18083 存取 | 我沒有 broker 主機權限 | 我有一台 IP 為 114.55.1.51 的 EMQX broker，請教我要如何在這台主機上啟用 HTTPS 18084，並把 HTTP 18083 限制成只有特定管理 IP 能存取。 |
| 3 | Broker 憑證重發 | 需要 Root CA 私鑰 | 產生含 SAN=Server/114.55.1.51 與 AKI 的憑證鏈 | 我沒有 CA 私鑰 | 我有一個私有 MQTT broker 在 114.55.1.51，目前憑證缺少 SAN 與 AKI，請教我如何重新產生包含 SAN 與 Authority Key Identifier 的憑證鏈。 |

---

## e. 預計執行項目

1. **Cloud UI 整合與重設計**：將 stores / OTA / settings / library、alerts / logs、dashboard / commands 整合，並參考 Pi 端 UI 重新設計版面（只改 UI，不改功能）。
2. **Pi 安裝驗證**：等待使用者回報 Pi 重新安裝結果，若仍失敗則繼續修復。
3. **Broker 安全強化**：待使用者處理 EMQX HTTPS 與憑證後，協助驗證連線。

---

## f. Linear 更新狀態

- 本 shell session 中沒有 `LINEAR_API_TOKEN`，未自動更新 Linear。
- 如需同步，請提供 token，或貼上：「修復 install.sh systemd 路徑並同步文件安裝網址，commit `02c5077` / `86e599a`」。

---

## g. 參考文件

- `install.sh`
- `README.md`
- `AGENTS.md`
- `OPERATIONS_MANUAL.md`
- `SECURITY_FINAL_REPORT.md`
