你現在要重做 NikkoMusicHub 的「系統設定」頁 UI。

請以我提供的「圖3」為唯一設計目標，盡可能 100% 還原圖3的整體版型、配色、卡片尺寸、間距、陰影、圓角、字體層級、按鈕樣式、輸入框樣式、Toggle 樣式與資訊排列。

圖1、圖2是目前系統 UI，代表現在的錯誤狀態。請不要沿用圖1圖2目前的排版，尤其不要保留右側那種獨立固定資訊欄、不要讓整頁被切成左右兩個 scroll container、不要讓頁面中間出現白色捲軸、不要讓 Toast 壓住主要內容、不要把首頁儀表板的狀態資訊塞到系統設定頁。

唯一不照抄圖3的地方：

左側 Sidebar 選單只保留三項：

1. 首頁儀表
2. 系統設定
3. 日誌紀錄

其他圖3左側選項，例如 Dashboard / Settings / Logs 英文選單，或者任何額外選項，都不要出現。  
但是主畫面內容、卡片布局、視覺風格、按鈕樣式、表格樣式，全部要照圖3還原。

---

## 一、整體版型要求

請把「系統設定」頁改成圖3那種完整、乾淨、現代的設定管理頁。

整體結構：

- 左側 Sidebar 固定寬度，大約 230px。
- 右側主內容區使用完整寬度。
- 不要再額外切出一條右側固定資訊欄。
- 不要讓主內容區中間出現白色垂直捲軸。
- 頂部 Header 高度約 72px。
- 主內容區使用 12 欄 Grid 排版。
- 主要內容寬度吃滿可用空間。
- 主內容 padding 約 24px。
- 卡片之間 gap 約 16px。
- 所有卡片都要對齊，不要高低錯亂。
- 整頁背景是深藍黑色，不要純黑。
- 卡片背景是深藍灰半透明質感，帶細邊框與輕微陰影。

請參考圖3的視覺風格：

- 背景：#07111f / #0b1628 類似深海軍藍
- Sidebar：#07111f 到 #0d1b2f 的深色漸層
- Card：#111d31 / #142238
- Border：rgba(100, 140, 190, 0.22)
- Primary Blue：#1e88ff / #2388ff
- Cyan Accent：#00b7ff
- Green Success：#22c55e
- Amber Warning：#f59e0b
- Red Danger：#ef4444
- Text Primary：#f8fafc
- Text Secondary：#94a3b8
- Text Muted：#64748b

整體感覺要像圖3：  
現代、精緻、深色 SaaS Settings Dashboard，不要像圖1圖2那種表單堆疊頁。

---

## 二、Sidebar 左側選單

請保留 NikkoMusicHub Logo。

Sidebar 結構：

上方：

- 音符 Logo + NikkoMusicHub
- 字體白色
- MusicHub 可以用亮藍色
- Sidebar 背景照圖3，深色、乾淨、有細邊線

選單只保留：

1. 首頁儀表
   - icon：home
   - 非 active

2. 系統設定
   - icon：settings / gear
   - active 狀態
   - 藍色高亮背景
   - 左側可以有一條亮藍色 active bar
   - 樣式要接近圖3 active item

3. 日誌紀錄
   - icon：file-text / clipboard / list
   - 非 active

底部：

- 保留 Logout 按鈕
- 保留版本資訊：
  - © 2024 NikkoMusicHub
  - All rights reserved.
  - 或 NikkoMusicHub v1.0.0

不要出現：

- Dashboard
- Settings
- Logs
- 音樂庫
- 播放清單
- 播放控制
- 系統狀態
- 維護工具
- 任何多餘選單

---

## 三、頂部 Header

請照圖3還原。

主內容頂部要有一條 Header。

左側：

- icon：齒輪 / settings
- 標題：系統設定
- 標題大字、白色、醒目

中間：

警告 Banner，文字如下：

「您正在使用預設密碼，請到 Settings 修改密碼。」

樣式：

- 深色底
- 黃橙色邊框
- 左側 warning icon
- 文字白色 / 淺色
- Settings 是可點擊 link 樣式
- Banner 位置在 Header 中間偏上
- 不要浮在右側資訊欄上
- 不要壓到卡片內容

右側：

- 綠色狀態 pill：
  - 系統狀態：正常
- 圓形 refresh icon button
- 圓形 user icon button

請移除圖1圖2頂部右側這些內容：

- 信義嘉興店
- Tailscale 100.69.3.7
- LAN 192.168.1.188

這些不要顯示在系統設定頁頂部。  
圖3沒有這些資訊，所以不要保留。

---

## 四、主畫面整體排列，必須照圖3

系統設定頁主內容請使用以下區塊順序。

請不要做成圖1圖2那種：

- 上面四張超高卡片
- 下面一張超長 WebDAV 卡
- 右邊固定資訊欄
- 中間白色捲軸

請改成圖3那種：

- 乾淨的設定頁
- 卡片式 Grid
- 第一排是設定核心卡
- 第二排是 WebDAV 與同步 / 初始化設定
- 底部是紀錄表格

---

## 五、第一排卡片

### 1. 店家資訊卡

位置：第一排左側

卡片標題：

- 店家資訊

內容欄位：

- 店名
- MQTT Store ID *

說明文字：

- Store ID 必須與 Cloud 平台新增的店點 Store ID 完全一致。

按鈕：

- 儲存店家資訊

資料：

請使用目前系統真實資料，不要硬改 demo。

例如目前資料：

- 店名：信義嘉興店
- MQTT Store ID：store-002

要求：

- 卡片尺寸接近圖3
- 不要太高
- 內容上下間距舒適
- 輸入框深色底、細邊框、圓角
- 儲存按鈕是藍色主按鈕
- icon 與標題放在同一行

---

### 2. 修改密碼卡

位置：第一排中間

卡片標題：

- 修改密碼

內容欄位：

- 目前密碼
- 新密碼

欄位右側：

- eye icon 顯示 / 隱藏密碼

按鈕：

- 修改密碼

要求：

- 卡片高度與店家資訊卡一致
- 不要做成超長空卡
- 按鈕寬度接近圖3
- 按鈕顏色是 Primary Blue
- 輸入框高度、圓角、邊框照圖3

---

### 3. 同步設定卡

位置：第一排偏右，主卡之一

卡片標題：

- 同步設定

內容：

- 同步模式 dropdown
  - sync（NAS 為主，本地同步）
- 啟用每日同步 toggle
- 每日同步時間
- 開機延遲同步（分鐘）
- 同步完成後自動重啟播放器 toggle

按鈕：

- 儲存同步設定

資料：

請保留目前系統真實資料，例如：

- 同步模式：sync（NAS 為主，本地同步）
- 每日同步時間：05:00
- 開機延遲同步：5

要求：

- 這張卡高度可以比左側卡片高一點，但整體要接近圖3
- toggle 要做成現代切換開關
- 表單說明文字不要太多，不要像圖1圖2那樣塞滿
- 內容排列要乾淨、均衡
- 儲存按鈕在卡片底部，寬度接近圖3

---

### 4. 手動操作卡

位置：第一排右側

卡片標題：

- 手動操作

按鈕從上到下：

1. Dry-run 測試同步
2. 立即同步
3. 清空本地音樂

顏色：

- Dry-run 測試同步：藍色
- 立即同步：綠色
- 清空本地音樂：紅色

要求：

- 按鈕寬度一致
- 按鈕高度一致
- 按鈕圓角一致
- 上下間距一致
- 不要塞很多說明文字
- 不要像圖1圖2那樣文字一大坨
- 卡片高度接近圖3

---

## 六、第二排卡片

### 5. WebDAV 連線卡

位置：第二排左側大卡  
這張卡必須是寬卡。

卡片標題：

- WebDAV 連線

欄位：

- Remote Name
- WebDAV URL
- Vendor
- Username
- Password
- Remote Music Path
- Local Music Path

按鈕：

1. 儲存 WebDAV 設定
2. 測試 WebDAV 連線
3. 列出 NAS 音樂檔案

請保留目前系統真實資料，例如：

- Remote Name：qnapmusic
- WebDAV URL：http://100.106.208.65:5005/
- Vendor：other
- Username：nikkolh
- Password：若已設定則顯示「已設定則留空保留原密碼」
- Remote Music Path：qnapmusic:NikkoMusic
- Local Music Path：/srv/nikko-music/music

要求：

- 欄位使用 2 欄或 3 欄排列，像圖3那樣乾淨
- 不要變成很長很散的表單
- 不要因為右側資訊欄擠壓而變窄，因為右側資訊欄必須刪除
- 按鈕放在卡片底部
- 主按鈕是藍色
- 次按鈕是深灰藍色
- 密碼欄位可保留目前提示文字

---

### 6. 系統初始化卡

位置：第二排右側或 WebDAV 旁邊，照圖3排列

卡片標題：

- 系統初始化

說明文字：

- 新安裝或更新依賴時使用，執行紀錄顯示於下方。

按鈕：

1. 系統更新（apt update + upgrade）
2. 安裝 rclone
3. 安裝 mpv
4. 建立音樂資料夾
5. 重新載入 systemd

要求：

- 位置照圖3
- 不要放在右側固定欄
- 不要拉成超長
- 按鈕樣式統一
- 可以使用兩欄按鈕排列
- 整體不要擠

---

## 七、底部紀錄區

### 7. 最近同步紀錄

位置：底部左側

卡片標題：

- 最近同步紀錄

右上角：

- 可以保留重新整理 / 查看全部按鈕

表格欄位：

- 時間
- 模式
- 狀態
- 同步項目
- 耗時
- 詳細資訊

如果目前沒有資料：

- 顯示「尚無同步紀錄」

要求：

- 做成圖3那種表格卡片
- 不要黑底大空框
- 要有表頭
- 空狀態要置中並有 icon
- 卡片高度與右側操作紀錄一致

---

### 8. 操作紀錄

位置：底部右側

卡片標題：

- 操作紀錄

表格欄位：

- 時間
- 使用者
- 操作
- 結果
- 詳細資訊

如果目前沒有資料：

- 顯示「尚無操作紀錄」

要求：

- 跟最近同步紀錄左右並排
- 卡片高度一致
- 表格樣式照圖3
- 不要只有一個黑色長條框

---

## 八、必須刪除的錯誤區塊

目前圖1圖2右側有一整條固定資訊欄，裡面包含：

- 店家資訊
- 系統資源
- 元件狀態
- 服務狀態
- NAS WebDAV 同步
- 播放器狀態
- 系統詳細資訊
- 軟體版本資訊

這些全部不要出現在系統設定頁。

原因：

- 圖3沒有右側固定資訊欄
- 這些是首頁儀表板資訊
- 系統設定頁只需要設定、操作、紀錄
- 不要把首頁狀態看板塞進系統設定頁

請直接移除相關 JSX / Component 引用。  
如果這些元件在首頁仍需使用，只要不要在系統設定頁 render 即可。

---

## 九、目前 UI 必須修掉的問題

請明確修掉以下問題：

### 1. 移除右側固定資訊欄

不要再有：

```tsx
<RightStatusPanel />
<StatusSidebar />
<SystemInfoRail />
<DashboardSidePanel />

或任何同類型右側固定資訊欄。

系統設定頁的 main layout 應該只有：

<Sidebar />
<main>
  <SettingsHeader />
  <SettingsGrid />
</main>
2. 修掉中間白色捲軸

目前圖1圖2中間有一條白色垂直捲軸，代表主畫面被拆成多個 scroll container。

請改成：

body / main 可以正常 scroll
不要在主內容中間再建立 overflow-y-scroll 的分欄
不要對右側資訊欄做 fixed + overflow-y-scroll，因為右側資訊欄要刪除
除非是表格或 log terminal，否則卡片不要自己出現捲軸

檢查 CSS：

移除不必要的 height: 100vh
移除不必要的 overflow-y: scroll
移除造成白色 scrollbar 的容器
需要 scrollbar 時請使用 dark scrollbar style
3. 警告 Banner 不要壓住內容

目前 warning toast 會浮在右側欄上，位置很差。

請改成圖3：

warning banner 在主內容 Header 中間
有固定高度
不遮住卡片
不浮在右側資訊欄上
z-index 不要亂壓內容
4. 卡片高度與間距統一

目前卡片高度太空、太散、太像硬塞。

請改成圖3：

卡片有固定內距
表單欄位高度一致
按鈕高度一致
卡片標題位置一致
12 欄 Grid 對齊
5. 不要改後端

只能改：

UI Layout
Component 排列
CSS / Tailwind class
資料顯示方式
Sidebar 選項

不要改：

API route
後端邏輯
rclone 邏輯
WebDAV 測試邏輯
同步流程
資料庫結構
既有事件名稱
十、CSS / Tailwind 方向

如果使用 Tailwind，請大致照以下風格。

整體：

<div className="min-h-screen bg-[#07111f] text-slate-100 flex">

Sidebar：

<aside className="w-[230px] shrink-0 bg-[#081426] border-r border-slate-700/40">

主內容：

<main className="flex-1 min-w-0 bg-[#07111f] px-6 py-5 overflow-y-auto">

Header：

<header className="h-[72px] flex items-center justify-between border-b border-slate-700/40">

卡片：

<section className="rounded-2xl border border-slate-700/50 bg-[#111d31]/95 shadow-[0_12px_30px_rgba(0,0,0,0.18)]">

卡片內距：

className="p-5"

卡片標題：

className="flex items-center gap-2 text-lg font-semibold text-slate-100"

說明文字：

className="text-sm text-slate-400"

輸入框：

className="h-10 rounded-lg border border-slate-700/60 bg-[#0b1424] px-3 text-sm text-slate-100 placeholder:text-slate-500 focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"

主按鈕：

className="h-10 rounded-lg bg-blue-600 px-4 text-sm font-semibold text-white hover:bg-blue-500"

次按鈕：

className="h-10 rounded-lg bg-slate-700/70 px-4 text-sm font-semibold text-slate-100 hover:bg-slate-600"

成功按鈕：

className="h-10 rounded-lg bg-green-600 px-4 text-sm font-semibold text-white hover:bg-green-500"

危險按鈕：

className="h-10 rounded-lg bg-red-500 px-4 text-sm font-semibold text-white hover:bg-red-400"

Grid 建議：

<div className="grid grid-cols-12 gap-4">
  <StoreInfoCard className="col-span-3" />
  <PasswordCard className="col-span-3" />
  <SyncSettingsCard className="col-span-3 row-span-2" />
  <ManualActionsCard className="col-span-3" />

  <WebDAVCard className="col-span-6" />
  <SystemInitCard className="col-span-3" />

  <SyncHistoryCard className="col-span-6" />
  <OperationLogCard className="col-span-6" />
</div>

如果實際寬度需要微調，可以調整 col-span，但最終畫面要像圖3。

十一、響應式要求

桌機版優先，因為我目前是在桌機上使用。

1440px 以上：

必須接近圖3
Sidebar 固定
主內容使用多欄 Grid

1024px～1439px：

可以稍微壓縮
仍然維持主要多欄設計

1024px 以下：

可以改成單欄或雙欄
但不要影響桌機版
十二、驗收標準

完成後我會用圖3對照檢查。

必須達成：

第一眼看起來就是圖3，不是圖1圖2。
Sidebar 只剩首頁儀表、系統設定、日誌紀錄三項。
Active 狀態是系統設定。
不再有右側固定資訊欄。
不再有中間白色垂直捲軸。
頂部警告 Banner 位置像圖3。
右上有系統狀態：正常 pill。
右上有 refresh icon button 與 user icon button。
第一排有店家資訊、修改密碼、同步設定、手動操作。
第二排有 WebDAV 連線與系統初始化。
底部有最近同步紀錄與操作紀錄。
所有卡片使用同一套深色卡片設計。
輸入框、按鈕、Toggle、表格都接近圖3。
不要顯示首頁儀表板才需要的系統資源、元件狀態、播放器狀態等資訊卡。
不要改 API，不要改後端，不要刪功能。
保留目前真實資料，不要清空表單。
修改後請提供截圖讓我比對。
十三、完成後回報

完成後請回報：

修改了哪些檔案
移除了哪些錯誤 UI 結構
如何移除右側固定資訊欄
如何修掉中間白色捲軸
如何限制 Sidebar 只顯示三項
是否保留所有既有按鈕功能
是否保留目前表單資料
請附上完成後截圖

最後再次強調：

除了 Sidebar 只保留「首頁儀表、系統設定、日誌紀錄」三項之外，其他全部照圖3還原。
不要自由發揮。
不要參考圖1圖2的排版。
不要保留右側固定資訊欄。
不要把首頁儀表板資訊塞進系統設定頁。
這次的目標是：100% 做成圖3那種系統設定頁。