# NikkoMusicHub 開發日報 — 2026-06-29

## 本輪目標
修復 Pi 安裝腳本、統一預設登入密碼、讓 MQTT TLS / broker 憑證驗證可選、更新 Cloud Vercel 環境變數與文件，並重新部署 Cloud Production；同時完成日前安排的 Cloud / Pi UI 調整。

## 完成項目

### 1. Cloud Vercel Production 部署與環境變數
- 已重新部署 Production：`https://cloud-vercel-xi.vercel.app`
- 統一 Cloud 登入帳密：`nikkolh` / `topup30%off`
- 已設定 / 更新環境變數：
  - `NIKKO_ADMIN_USER=nikkolh`
  - `NIKKO_ADMIN_PASS=topup30%off`
  - `NIKKO_MQTT_COMMAND_SECRET=topup30%off`
  - `NIKKO_MQTT_TOPIC_PREFIX=nikko`
  - `NIKKO_MQTT_TLS_VERIFY=0`
- 本地開發用 `.env.production.local` 也同步更新為相同帳密。
- Build 驗證：`Compiled successfully`、`Generating static pages (25/25)`。
- 登入 API 已驗證回傳 HTTP 200。

### 2. Linear 更新
- 已建立並標記為 Done：`STE-106` — "Fix Pi install script, unify default login, optional MQTT TLS, Cloud/Pi UI improvements"
- 連結：https://linear.app/nikkolh/issue/STE-106

### 3. MQTT 連線與認證修復（Cloud）
- `cloud-vercel/lib/mqtt.js`
  - 若店點未填 broker/user/pass，自動回退到內部預設值（`114.55.1.51:1883`，`admin` / `topup30%off`）。
  - TLS 與憑證驗證改為透過環境變數可選關閉。
  - 修正 command payload 的傳遞與簽名方式，使 `webdav_list_music` 等需要 payload 的指令可正常執行。
- `cloud-vercel/app/api/settings/route.js`
  - 回傳設定時自動補上 MQTT / NAS WebDAV 預設值，前端輸入框顯示為白色已填寫狀態。

### 4. Pi 安裝腳本統一
- `install.sh`
  - 新安裝與 repair 流程皆使用統一的 MQTT 預設值：
    - broker `114.55.1.51:1883`
    - username `admin` / password `topup30%off`
    - `COMMAND_SECRET=topup30%off`
    - `TOPIC_PREFIX=nikko`
    - `TLS=0`、`TLS_VERIFY=0`
  - 腳本語法已通過 `bash -n` 檢查。

### 5. Cloud UI 調整
由 subagent 完成，主要變更：
- **監控告警**：每列加上 checkbox、頂端「批量標記已處理」按鈕、已處理列顯示灰色「已處理」按鈕；店點欄位顯示店名 + Store ID。
- **店點列表**：新增店點時輸入自動加上 `store-` 前綴；MQTT 密碼加入眼睛顯示/隱藏切換；預設 broker 資訊以實際值預填。
- **遠端 log**：載入按鈕與輸入框對齊，並補上 tooltip。
- **音樂庫**：移除「下載到選取店點」按鈕；NAS 設定密碼加入眼睛切換，URL / Remote Path 以預設值預填。
- **OTA**：顯示 GitHub `security-final` 最新 commit 版本，並透過 `status_system` 取得各店點目前版本。
- **總覽控制台**：將店點列表與中央控制台合併成單一卡片，控制台置於頁面下方；按鈕統一加 tooltip。
- **Settings**：預設 MQTT 密碼加入眼睛切換，預設值以實際值預填。

### 6. Pi UI / Backend 調整
由 subagent 完成，主要變更：
- `app/templates/dashboard.html`
  - 右側多張狀態卡片合併，移除獨立的「播放器狀態」卡片。
  - 播放器控制台精簡：移除隨機/循環重複 icon、移除音訊裝置選擇與測試按鈕，改為單鍵靜音/取消靜音切換，音量滑桿下方顯示標籤。
- `app/static/style.css`：新增設定表單按鈕最小寬度、音量標籤與響應式樣式。
- `app/templates/base.html`
  - 靜態資源 cache busting 更新為 `v=20260630-01`。
  - 登出文字改為「登出系統」並加 tooltip。
- `app/templates/settings.html`
  - MQTT 欄位以預設值白色預填。
  - WebDAV 欄位重排，移除 Vendor，路徑改為 `/NikkoMusic`；精簡按鈕保留 Dry-run 與立即同步，提交按鈕固定寬度。
  - 新增「網路設定」卡片：可設定 WiFi 與網路優先順序（透過 `nmcli` 嘗試套用）。
- `app/routes/player.py`：新增 `/api/player/mute-toggle`。
- `app/services/mpv.py`：`get_status()` 回傳 `mute` 狀態。
- `app/routes/settings.py` / `app/routes/webdav.py`：提供 MQTT / 網路預設值，Remote Path 顯示改為 `/`。
- `app/routes/network.py`（新增）：`/api/settings/network` GET/POST，儲存並套用 WiFi / 優先順序。
- `app/main.py`：註冊 `network` router。
- `app/routes/version.py`：擴充 Changelog，補上 install script、WebDAV、播放器、MQTT、儀表板、網路設定等歷程。
- 所有變更的 `.py` 檔案已通過 `python -m py_compile` 語法檢查。

### 7. 文件整理
- 根目錄 `260619-*.md`、`260629-*.md` 已移至 `record/`。

## 尚待完成 / 注意事項
1. **Pi 端實地更新**
   - 程式碼已 push 到 `security-final`，`scripts/repair-pi.sh` 會拉取最新 commit。
   - 若你在 push 之前已執行過 repair，請**重新執行**以套用本次修改：
     ```bash
     sudo bash scripts/repair-pi.sh
     ```
   - 執行後會套用新的 `nikko.env`（MQTT broker、command secret、topic prefix）與最新 UI。

2. **Linear 更新**
   - 已使用提供的 Linear Token 建立並標記 Done：`STE-106`
   - 連結：https://linear.app/nikkolh/issue/STE-106

3. **Preview 環境管理員帳密**
   - Production 與 Preview 的 `NIKKO_ADMIN_USER` / `NIKKO_ADMIN_PASS` 皆已透過 Vercel API Token 設為 `nikkolh` / `topup30%off`。

## 驗證指令
```bash
# Cloud 登入
curl -X POST https://cloud-vercel-xi.vercel.app/api/auth \
  -H 'Content-Type: application/x-www-form-urlencoded' \
  -d 'username=nikkolh&password=topup30%off'
# 預期回傳 HTTP 200

# Pi 語法檢查（本地）
python -m py_compile app/main.py app/routes/player.py app/routes/settings.py \
  app/routes/version.py app/routes/webdav.py app/services/mpv.py app/routes/network.py

# Cloud build（本地）
cd cloud-vercel && npm run build
```

## 結論
Cloud Production 已重新部署並可透過統一帳密 `nikkolh` / `topup30%off` 登入；MQTT 認證、TLS 選項、預設值回退與 UI 調整皆已上線並 push 到 `security-final`。Linear 已建立 `STE-106` 並標記 Done。Pi 端程式碼已更新且通過語法檢查，下一步需在實體 Pi 執行 `repair-pi.sh` 以套用變更。
