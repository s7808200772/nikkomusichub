# NikkoMusicHub 專案進度回報｜260619_06

> 本輪補齊 CLI token、更新 Linear、補上 Cloud Vercel 的 `NIKKO_MQTT_TLS_VERIFY` 環境變數並重新部署 Production。

---

## a. 整體專案進度

| 狀態 | 數量 | 比例 |
|---|---|---|
| 已完成 | 108 | 90.0% |
| 部分完成 | 1 | 0.8% |
| 暫緩（未開始） | 11 | 9.2% |

---

## b. 本次變動項目

| 序號 | 項目 | 改變內容 | 如何驗收 | 預期效果 |
|---|---|---|---|---|
| 1 | 補上 CLI token | Linear / Vercel / Supabase token 已寫入當前 shell 使用 | 可成功執行 `linearis projects update` 與 `vercel env add` | 後續自動更新專案管理與部署 |
| 2 | 更新 Linear | `NikkoMusicHub` 專案已新增進度更新 | 在 Linear 可看到最新 project update | 專案管理與實際進度同步 |
| 3 | 新增 Vercel env | Production 與 Preview 皆新增 `NIKKO_MQTT_TLS_VERIFY=1` | `vercel env ls` 可看到該變數 | 可透過將值改為 `0` 關閉 broker TLS 憑證驗證 |
| 4 | 重新部署 Cloud | 因新增 env，已重新執行 `vercel --prod` | `https://cloud-vercel-xi.vercel.app` 正常運作 | 線上環境套用最新設定 |

---

## c. 目前遺留問題

| 序號 | 問題 | 不處理影響 | 重要 | 風險 | 優先 |
|---|---|---|---|---|---|
| 1 | EMQX Dashboard 仍走 HTTP 18083 | 管理密碼可能在公開網路被竊聽 | ★★★★★ | ★★★★☆ | ★★★★★ |
| 2 | Broker 憑證缺少 SAN/AKI | 未來可能連不上；目前可用 `NIKKO_MQTT_TLS_VERIFY=0` 繞過 | ★★★★☆ | ★★★☆☆ | ★★★☆☆ |
| 3 | Pi 安裝與登入尚未驗收 | 不確定 Pi 端是否正常 | ★★★★★ | ★★★★☆ | ★★★★★ |

---

## d. 需我處理項目

| 序號 | 項目 | 需要你執行 | 為什麼我無法自己做 | 給 GPT 的 prompt |
|---|---|---|---|---|
| 1 | Pi 重新安裝並驗證 | `curl -fsSL https://raw.githubusercontent.com/s7808200772/nikkomusichub/security-final/install.sh \| sudo bash` <br> 完成後開 `http://100.69.3.7:8080` 用 `nikkolh` / `topup30%off` 登入 | 無法 SSH 到你的 Pi | 我在 Pi 用 nikkolh SSH 登入了，請幫我執行 `curl -fsSL https://raw.githubusercontent.com/s7808200772/nikkomusichub/security-final/install.sh \| sudo bash`，確認顯示安裝完成與預設密碼，再開瀏覽器測試登入。 |
| 2 | EMQX HTTPS / IP 限制 | 在 114.55.1.51 啟用 HTTPS 18084 或限制 18083 | 沒有 broker 主機權限 | 我有一台 IP 114.55.1.51 的 EMQX broker，請教我要如何啟用 HTTPS 18084 並把 18083 限制成只有管理 IP 能存取。 |
| 3 | Broker 憑證重發 | 產生含 SAN/AKI 的憑證鏈 | 沒有 CA 私鑰 | 我有一個私有 MQTT broker 在 114.55.1.51，憑證缺少 SAN 與 AKI，請教我如何重新產生包含這兩項的憑證鏈。 |

---

## e. 預計執行項目

1. 等待使用者回報 Pi 重新安裝與登入結果；若仍失敗則繼續修復。
2. Pi 正常後測試 Cloud → Pi MQTT 指令與同步。
3. 安排 broker 主機處理 EMQX HTTPS 與憑證問題。

---

## f. Linear 更新狀態

- ✅ 已使用 `LINEAR_API_TOKEN` 更新 `NikkoMusicHub` 專案 project update。
- 內容：Cloud UI 整合完成、Vercel env 補齊、Production 重新部署、commit `623b90a`。

---

## g. 參考文件

- `cloud-vercel/README.md`
- `AGENTS.md`
- `260619_05.md`
