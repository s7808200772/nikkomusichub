# 260629_13 修復紀錄

## 本次目標
全面修復 Pi / Cloud 功能與 UI，完成 260629_13 批次需求。

## Cloud 端變更

1. **看門狗批量安裝/停用與參數設定**
   - `cloud-vercel/app/api/watchdog/route.js`：install / bulk install 現在接收 `target`、`interval`、`threshold`，並透過 MQTT payload 傳給 Pi。
   - `cloud-vercel/app/stores/StoresClient.js`：新增「看門狗參數」摺疊面板，可設定 ping 目標、檢查間隔、連續失敗次數；單店與批量安裝均會帶入參數。

2. **店點指令控制台藍色 icon 崩潰修復**
   - `cloud-vercel/components/ResponseFormatter.js`：新增 `FormatterErrorBoundary`，任何格式化異常都會顯示友善錯誤而不是白屏；補強 `fmt()`、服務狀態、Git Commit、Python 版本等非字串值處理。

3. **同步 NAS WebDAV 人話輸出**
   - `app/services/rclone.py`：`sync_music()` 回傳結果加入人話 `message`（同步完成 / 同步失敗 / 連線測試完成），Cloud 指令控制台不再顯示原始 rclone verbose log。

4. **Dashboard / 系統資訊中文與排版**
   - `ResponseFormatter.js` 已全中文標籤與分區卡片，`SystemFormatter`、`DashboardFormatter` 排版採用 `resp-grid` 自適應欄位。

5. **最近告警每店點只顯示最新一筆**
   - `cloud-vercel/app/DashboardClient.js`：使用 `useMemo` 依 `store_id` 分組取最新，再依時間排序取前 5 筆；標題改為「最近告警（每店點最新一筆）」。

6. **版本更新加入日期**
   - `app/services/system.py`：`get_git_version()` 增加 commit date。
   - `app/routes/version.py` 與 `app/templates/version.html`：目前版本卡片顯示 Commit 日期。
   - `cloud-vercel/app/ota/OtaClient.js`：店點版本若含 date 會顯示 `commit (branch) · 日期`。

7. **音樂庫按鈕數字改白色**
   - `cloud-vercel/app/globals.css` 新增 `.badge-white`。
   - `cloud-vercel/app/library/LibraryClient.js`：目標店點與 NAS 音樂清單的數量徽章改用白字黑底。

## Pi 端變更

1. **首頁儀表板 QNAP/Tailscale 載入修復**
   - `app/templates/dashboard.html`：`loadConnectivity` 加入 timeout 與錯誤分支，避免未設定時無限載入。

2. **音樂服務狀態隨播放更新**
   - `app/templates/dashboard.html`：當 `player_status === 'playing'` 時，音樂服務狀態顯示「播放中」並亮綠燈。

3. **版本更新真實版本與日期**
   - 見上方 Cloud 第 6 項與 `app/services/system.py` / `app/routes/version.py` / `app/templates/version.html`。

4. **系統初始化按鈕排列與人話回應**
   - `app/templates/settings.html`：系統初始化改為 3 行 2 欄排列。
   - `app/routes/setup.py`：apt / rclone / mpv / 資料夾 / systemd  reload 等回應改為人話；sudo 密碼錯誤會明確提示。

5. **看門狗預設 8.8.8.8/300/5**
   - `app/services/watchdog.py`：`install_watchdog` 預設改為 300 秒 / 5 次；`app/mqtt_client.py` 安裝時會帶入 Cloud 傳來的參數，預設即為 300/5。

6. **WebDAV 密碼已填顯示 `********`**
   - `app/routes/settings.py`：傳入 `webdav_password_set`。
   - `app/templates/settings.html`：WebDAV 密碼欄位已設定時顯示 `********`。
   - `app/routes/webdav.py`：儲存時若收到 `********` 視為「不變更」，避免覆蓋真實密碼。

7. **頂部時間與系統狀態 icon 修復**
   - `app/static/app.js`：頂部導覽列加入 `nav-clock`，每秒更新 `YYYY-MM-DD HH:MM:SS`。

8. **播放控制台波形與版面調整**
   - `app/static/style.css`：波形效果改到 `.cover.waveform`，版面更集中。

9. **所有按鈕文字置中、標題置中**
   - `app/static/style.css`：`.maintenance-col button` 與 `.card h2, .card h3` 改為置中；`.card-header` 內標題維持左對齊避免操作列跑版。

## 追加修正（依使用者回饋檢查後）

- `cloud-vercel/components/ResponseFormatter.js`：所有標籤改為中文（Git Commit → Git 提交、LAN IP → 區網 IP、RAM → 記憶體、同步 Timer → 同步排程狀態等）。
- `cloud-vercel/app/commands/CommandsClient.js`：指令控制台「Dashboard」按鈕改為「儀表板」。
- `cloud-vercel/app/DashboardClient.js`：最近告警每店最新一筆不再限 5 筆，改為顯示全部最新。
- `cloud-vercel/app/ota/OtaClient.js`：版本號解析補上 `parsed.date` 顯示日期。

## 驗證

- `python -m py_compile`：Pi 端修改檔案語法檢查通過。
- `cd cloud-vercel && npm run build`：本地 build 成功。
- `cd cloud-vercel && vercel --prod`：已重新部署至 Production（alias `https://cloud-vercel-xi.vercel.app`）。
- Pi 本機 `scripts/test-suite.py` 因 Windows 環境無法模擬 Pi 環境，僅部分項目通過（屬預期）。

## 後續

- Pi 端需透過 `scripts/repair-pi.sh` 或手動 `git pull` + `systemctl restart ...` 套用變更。
- Linear STE-107 已標記為 Done，並建立 project update（進度 95%）。
