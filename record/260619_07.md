# NikkoMusicHub 專案進度回報｜260619_07

> 本輪針對 Pi 安裝後無法連線、且 `initial-admin-password` 檔案缺失的問題進行修復與補強。

---

## a. 整體專案進度

| 狀態 | 數量 | 比例 |
|---|---|---|
| 已完成 | 109 | 90.8% |
| 部分完成 | 1 | 0.8% |
| 暫緩（未開始） | 10 | 8.4% |

---

## b. 本次變動項目

| 序號 | 項目 | 改變內容 | 如何驗收 | 預期效果 |
|---|---|---|---|---|
| 1 | 強制產生初始密碼檔 | `install.sh` 現在只要 `initial-admin-password` 不存在就一定會建立，不再只依賴 env 變數是否缺失 | 重新安裝後 `cat /srv/nikko-music/data/initial-admin-password` 會顯示 `topup30%off` | 預設密碼檔一定存在 |
| 2 | 安裝後檢查 web 服務 | `install.sh` 啟動服務後會等待 2 秒並檢查 `nikko-music-hub-web.service` 是否成功啟動；失敗會印出最近 log | 安裝腳本結尾會顯示服務狀態，不再假裝成功 | 更早發現服務啟動失敗 |
| 3 | 新增診斷腳本 | `scripts/diagnose-pi.sh` 可檢查目錄、env、systemd 單元、監聽埠、服務 log、網路位址 | 在 Pi 執行 `curl .../scripts/diagnose-pi.sh \| sudo bash` 會印出完整資訊 | 快速定位 Pi 問題 |
| 4 | 新增修復腳本 | `scripts/repair-pi.sh` 會補上密碼檔、修正權限、重新安裝 systemd 單元並重啟服務 | 在 Pi 執行 `curl .../scripts/repair-pi.sh \| sudo bash` 後嘗試開網頁 | 自動修復常見安裝遺漏 |

---

## c. 目前遺留問題

| 序號 | 問題 | 不處理影響 | 重要 | 風險 | 優先 |
|---|---|---|---|---|---|
| 1 | EMQX Dashboard 仍走 HTTP 18083 | 管理密碼可能在公開網路被竊聽 | ★★★★★ | ★★★★☆ | ★★★★★ |
| 2 | Broker 憑證缺少 SAN/AKI | 未來可能連不上；目前可用 `NIKKO_MQTT_TLS_VERIFY=0` 繞過 | ★★★★☆ | ★★★☆☆ | ★★★☆☆ |
| 3 | Pi 安裝後仍無法連線（本次回報） | 無法使用 Pi 端功能 | ★★★★★ | ★★★★☆ | ★★★★★ |

---

## d. 需我處理項目

| 序號 | 項目 | 沒完成原因 | 需要你執行 | 為什麼我無法自己做 | 給 GPT 的 prompt |
|---|---|---|---|---|---|
| 1 | 在 Pi 執行修復腳本並回報結果 | 需要 SSH 到 Pi 並有 sudo 權限 | 執行：`curl -fsSL https://raw.githubusercontent.com/s7808200772/nikkomusichub/security-final/scripts/repair-pi.sh \| sudo bash` <br> 完成後開 `http://100.69.3.7:8080` 測試；若仍失敗，再執行診斷腳本：`curl -fsSL https://raw.githubusercontent.com/s7808200772/nikkomusichub/security-final/scripts/diagnose-pi.sh \| sudo bash` <br> 把輸出貼回來 | 無法直接 SSH 到你的 Pi | 我在 Raspberry Pi 用 nikkolh SSH 登入了，請幫我執行 `curl -fsSL https://raw.githubusercontent.com/s7808200772/nikkomusichub/security-final/scripts/repair-pi.sh \| sudo bash`，完成後開瀏覽器測試 `http://100.69.3.7:8080` 是否能連上。 |
| 2 | EMQX HTTPS / IP 限制 | 需要 broker 主機權限 | 在 114.55.1.51 啟用 HTTPS 18084 或限制 18083 | 沒有 broker 主機權限 | 我有一台 IP 114.55.1.51 的 EMQX broker，請教我要如何啟用 HTTPS 18084 並把 18083 限制成只有管理 IP 能存取。 |
| 3 | Broker 憑證重發 | 需要 Root CA 私鑰 | 產生含 SAN/AKI 的憑證鏈 | 沒有 CA 私鑰 | 我有一個私有 MQTT broker 在 114.55.1.51，憑證缺少 SAN 與 AKI，請教我如何重新產生包含這兩項的憑證鏈。 |

---

## e. 預計執行項目

1. 根據你執行修復/診斷腳本後的回報，判斷 Pi 無法連線的真正原因（服務未啟動、port 被佔用、Tailscale IP 變更、Python 啟動錯誤等）。
2. 針對診斷結果繼續修復 install.sh 或 Pi 設定。
3. Pi 連線正常後，測試 Cloud → Pi MQTT 指令。

---

## f. Linear 更新狀態

- ✅ 已更新 `NikkoMusicHub` 專案 project update：Pi 無法連線 / 密碼檔缺失修復，新增診斷與修復腳本，commit `f15a60f`。

---

## g. 參考文件

- `install.sh`
- `scripts/diagnose-pi.sh`
- `scripts/repair-pi.sh`
- `260619_06.md`
