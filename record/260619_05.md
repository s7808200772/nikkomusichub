# NikkoMusicHub 專案進度回報｜260619_05

> 本輪完成 Cloud UI 整合與重設計，參考 Pi 端風格改成左側側邊欄，並將原本 8 個頁面整合為 3 個主要入口。

---

## a. 整體專案進度

| 狀態 | 數量 | 比例 |
|---|---|---|
| 已完成 | 107 | 89.2% |
| 部分完成 | 1 | 0.8% |
| 暫緩（未開始） | 12 | 10.0% |

**備註子項目**：
- Cloud UI 整合重設計：已完成並部署。
- Pi 安裝與文件修復：已完成。
- Pi 登入驗證：等待使用者在 Pi 重新執行安裝後確認。

---

## b. 本次變動項目

| 序號 | 項目 | 改變內容 | 如何驗收 | 預期效果 |
|---|---|---|---|---|
| 1 | 新增左側側邊欄 | 用 `Sidebar` + `AppShell` 取代原本的頂部 `Navbar`，配色與 Pi 端一致 | 登入 Cloud 後左側出現「總覽控制台 / 店點管理 / 監控與紀錄」選單 | 導航更貼近 Pi 使用體驗 |
| 2 | 整合 Dashboard 與 Commands | `/` 現在同時顯示狀態卡片與遠端指令控制台；`/commands` 自動導回 `/` | 開啟 `https://cloud-vercel-xi.vercel.app` 可看到統計卡片與指令按鈕 | 不用切頁就能監控與下指令 |
| 3 | 整合 Store / Library / OTA / Settings | `/stores` 用分頁切換：店點列表、音樂庫、OTA、預設 Broker；舊頁面導回 `/stores` | 開啟 `/stores` 可切換四個分頁 | 店點相關操作集中在一處 |
| 4 | 店點列表改為條列式 | 已登錄店點從卡片改成 `.list-table` 表格，編輯在表格下方展開 | `/stores` 的店點列表是緊湊表格 | 節省版面，資訊一目瞭然 |
| 5 | 整合 Alerts 與 Logs | 新增 `/monitoring`，用分頁切換告警中心與遠端 Log；舊 `/alerts`、`/logs` 自動導回 | 開啟 `/monitoring` 有兩個分頁 | 監控相關資訊集中 |
| 6 | 更新 Cloud 文件 | `cloud-vercel/README.md` 與 `AGENTS.md` 更新為新的頁面結構與路由 | 文件中的功能描述與目前介面一致 | 文件不再指向舊頁面 |
| 7 | 部署 Cloud Production | `vercel --prod` 成功 | `https://cloud-vercel-xi.vercel.app` 可正常開啟 | 線上環境已套用新 UI |

---

## c. 目前遺留問題

| 序號 | 問題 | 不處理影響 | 重要 | 風險 | 優先 |
|---|---|---|---|---|---|
| 1 | EMQX Dashboard 仍走 HTTP 18083 | 管理密碼可能在公開網路被竊聽 | ★★★★★ | ★★★★☆ | ★★★★★ |
| 2 | Broker 憑證缺少 SAN/AKI | 未來可能連不上；目前可用 `NIKKO_MQTT_TLS_VERIFY=0` 繞過 | ★★★★☆ | ★★★☆☆ | ★★★☆☆ |
| 3 | Pi 安裝與登入尚未驗收 | 不確定 Pi 端是否可正常運作 | ★★★★★ | ★★★★☆ | ★★★★★ |

---

## d. 需我處理項目

| 序號 | 項目 | 沒完成原因 | 需要你執行 | 為什麼我無法自己做 | 給 GPT 的 prompt |
|---|---|---|---|---|---|
| 1 | Pi 重新安裝並驗證登入 | 需要 SSH 到 Pi 並有 sudo 權限 | 執行：`curl -fsSL https://raw.githubusercontent.com/s7808200772/nikkomusichub/security-final/install.sh \| sudo bash` <br> 完成後開 `http://100.69.3.7:8080` 用 `nikkolh` / `topup30%off` 登入 | 無法直接連進你的 Pi | 我在 Pi 用 nikkolh SSH 登入了，請幫我執行 `curl -fsSL https://raw.githubusercontent.com/s7808200772/nikkomusichub/security-final/install.sh \| sudo bash`，確認顯示安裝完成與預設密碼，再開瀏覽器測試登入。 |
| 2 | EMQX HTTPS / IP 限制 | 需要 broker 主機權限 | 在 114.55.1.51 啟用 HTTPS 18084 或限制 18083 | 沒有 broker 主機權限 | 我有一台 IP 114.55.1.51 的 EMQX broker，請教我要如何啟用 HTTPS 18084 並把 18083 限制成只有管理 IP 能存取。 |
| 3 | Broker 憑證重發 | 需要 Root CA 私鑰 | 產生含 SAN/AKI 的憑證鏈 | 沒有 CA 私鑰 | 我有一個私有 MQTT broker 在 114.55.1.51，憑證缺少 SAN 與 AKI，請教我如何重新產生包含這兩項的憑證鏈。 |

---

## e. 預計執行項目

1. 等待使用者回報 Pi 重新安裝與登入結果；若仍失敗，繼續修復 install.sh 或 Pi 設定。
2. 待 Pi 可正常運作後，測試 Cloud → Pi 的 MQTT 指令與同步。
3. 安排 broker 主機處理 EMQX HTTPS 與憑證問題。

---

## f. Linear 更新狀態

- 本 shell session 中沒有 `LINEAR_API_TOKEN`，未自動更新 Linear。
- 如需同步，請提供 token，或貼上：「Cloud UI 整合重設計完成，sidebar + dashboard/commands + stores hub + monitoring hub，Production 已部署，commit `cc4ce89`」。

---

## g. 參考文件

- `cloud-vercel/README.md`
- `AGENTS.md`
- `cloud-vercel/app/globals.css`
- `cloud-vercel/components/Sidebar.js`
- `cloud-vercel/app/stores/StoresHubClient.js`
- `cloud-vercel/app/monitoring/MonitoringClient.js`
